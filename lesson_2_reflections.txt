What happens when you initialize a respository?  Why do you need to do it?

It creates a new repository with all the metadata.  You need it for storing 
versions/doing commit and running almost all other git commands
